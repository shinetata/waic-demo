verify-deps-before-run=false

# pnpm store 固定在本仓库内，避免在父目录（WAIC）生成 .pnpm-store。
# 注：pnpm 对相对 store-dir 支持不稳定；安装依赖请用 `./scripts/dev.sh install`
# （脚本用 --store-dir 显式指向仓库内 .pnpm-store，权威且可移植）。
store-dir=.pnpm-store
